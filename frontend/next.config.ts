import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Let Vercel handle bundling natively
  // Add any rewrites, redirects, or headers here as needed
};

export default nextConfig;
